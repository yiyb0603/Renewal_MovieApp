import React from 'react';
import './Loading.scss';

const Loading = () => {
    return (
        <div className ="Loading">
            <span>Loading....</span>
        </div>
    );
}

export default Loading;